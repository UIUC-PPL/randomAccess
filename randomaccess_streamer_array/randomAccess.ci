mainmodule randomAccess {
    readonly CProxy_Main    mainProxy;
    readonly int            N;
    readonly CmiInt8        localTableSize;
    readonly CmiInt8        tableSize;
    readonly CProxy_ArrayMeshStreamer<CmiUInt8> aggregator; 
    readonly CProxy_CompletionDetector detector;
    
    mainchare Main {
        entry Main (CkArgMsg*);
        entry [reductiontarget] void start();
        entry void allUpdatesDone();
        entry [reductiontarget] void verifyDone(CmiInt8);
    };
    
    array [1D] Updater: MeshStreamerArrayClient<CmiUInt8> {
        entry Updater();
        entry [threaded] void  generateUpdates();
        entry void checkErrors();
    };

    message MeshStreamerMessage<ArrayDataItem<CmiUInt8> >;
    array [1D] MeshStreamerArrayClient<CmiUInt8>;
    group ArrayMeshStreamer<CmiUInt8>;    
    group MeshStreamer<ArrayDataItem<CmiUInt8> >;

};
